# Neocortical Localization and Thalamocortical Modulation of 
# Neuronal Hyperexcitability contribute to Fragile X Syndrome
# Communication Biology 
# Script Runfile
# Author: E. Pedapati
# Version: 3/27/2022

source("commbio_R_demographics.R") 
rm(list = ls())

source("commbio_R_preprocessing.R")
rm(list = ls())

source("commbio_R_PeakFreqSource.R")
rm(list = ls())

source("commbio_R_SrcPowFig3.R")
rm(list = ls())

source("commbio_R_AAC.R")
rm(list = ls())
